package com.example.oracle;

import com.example.core.storage.PersistentStorage;

public class CloudStorageSyncAdapter extends SyncAdapter {
    public CloudStorageSyncAdapter(Object p0, PersistentStorage persistentStorage) {
        super();
    }
}
